@extends('layouts.app')
@section('title','Laporan Magang')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Laporan Magang</h1><p class="mt-1 text-sm text-slate-500">Tinjau dan validasi laporan mahasiswa bimbingan.</p></div>
<div class="overflow-x-auto rounded-xl border bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Mahasiswa</th><th class="p-4 text-left">File</th><th class="p-4">Upload</th><th class="p-4">Status</th><th class="p-4">Aksi</th></tr></thead><tbody class="divide-y">@forelse($laporan as $item)<tr class="align-top"><td class="p-4"><b>{{ $item->magang?->mahasiswa?->nama }}</b><div class="text-xs text-slate-500">{{ $item->magang?->mahasiswa?->nim }}</div></td><td class="p-4"><a class="font-semibold underline" href="{{ route('dosen.laporan.download',$item) }}">{{ $item->nama_file }}</a></td><td class="p-4">{{ $item->tanggal_upload?->format('d M Y H:i') }}</td><td class="p-4"><x-badge :status="$item->status"/></td><td class="min-w-[240px] p-4">@if($item->status==='belum_validasi')<form method="POST" action="{{ route('dosen.laporan.validate',$item) }}" class="space-y-2">@csrf @method('PATCH')<select name="status" class="w-full rounded-lg border p-2 text-sm" required><option value="">Pilih keputusan</option><option value="disetujui">Setujui</option><option value="ditolak">Tolak</option></select><textarea name="catatan_dosen" rows="2" placeholder="Catatan, wajib jika ditolak" class="w-full rounded-lg border p-2 text-sm"></textarea><button class="w-full rounded-lg bg-slate-900 px-3 py-2 text-xs font-semibold text-white">Simpan</button></form>@else<p class="text-xs text-slate-500">{{ $item->catatan_dosen ?: 'Tidak ada catatan.' }}</p>@endif</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">Belum ada laporan.</td></tr>@endforelse</tbody></table></div>{{ $laporan->links() }}</div>
@endsection